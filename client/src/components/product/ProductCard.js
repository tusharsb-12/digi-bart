import React from 'react'
import './product.css'

const ProductCard = ({ product }) => {
    const { title, descriptions, images, value, category, postedOn, bougtOn, owner, condition, shippingAvailablity, upForTrade } = product;
    return (
        <div>

        </div>
    )
}
export default ProductCard